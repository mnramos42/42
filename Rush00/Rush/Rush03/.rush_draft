void    ft_putchar(char c);

void    rush(int x, int y)
{
    int coluna_x;
    int linha_y;

    coluna_x = x;
    linha_y = y;

    while(linha_y > 0)
    {
        if(linha_y == y | linha_y == 1){
            ft_putchar('A');
            coluna_x--;
        }

        while(coluna_x > 0)
        {
            if(coluna_x == x | coluna_x == 1 | linha_y == y | linha_y == 1)
            {
                if((coluna_x == 1 & linha_y == y) | (coluna_x == 1 & linha_y == 1))
                {
                    ft_putchar('C');
                }

                else
                {
                    ft_putchar('B');
                }
            }
            else
            {
                ft_putchar(' ');
            }
            coluna_x--;
        }
        coluna_x = x;
        linha_y--;
        ft_putchar('\n');
    }
}

void	ft_putchar(char c);

void	rush(int x, int y)
{

	while (x>0){
		ft_putchar (A)
		}
		while (x>1){
			ft_putchar (B)
			}	
		ft_putchar (C)
				

		ft_putchar ()

	}
	
}
